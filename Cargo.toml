[workspace]

members = [
    "cached_hash_map",
    "chunked_grid",
    "nzslab",
    "physics",
    "self_organizing_list",
]


[profile.bench]
lto = true
panic = "abort"

[profile.release]
lto = true
panic = "abort"
